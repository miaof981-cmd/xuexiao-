<!DOCTYPE html>
<html lang="zh-CN">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>登录 - 次元学院</title>
  <link rel="stylesheet" href="/styles.css" />
  <style>
    body { position: relative; min-height: 100vh; display: grid; place-items: center; background: linear-gradient(135deg, #5aa7ff 0%, #2c72ff 100%) fixed; }
    .admission-bg { position:absolute; top:24px; left:24px; transform: rotate(-8deg); opacity:.35; z-index:0; width:min(520px, 50vw); pointer-events:none; filter: drop-shadow(0 8px 20px rgba(0,0,0,.2)); }
    .login-card { width: 420px; max-width: 92vw; background: rgba(255,255,255,.96); border-radius: 14px; box-shadow: 0 12px 32px rgba(0,0,0,.18); overflow: hidden; position: relative; z-index: 1; }
    .login-header { padding: 24px 28px; background: linear-gradient(90deg,#f0f4ff,#fff); border-bottom: 1px solid #eef; }
    .login-title { margin: 0 0 8px; font-size: 20px; color: #111827; }
    .tabs { display: flex; }
    .tab { flex: 1; text-align: center; padding: 12px; cursor: pointer; font-weight: 700; border-bottom: 2px solid transparent; font-size: 15px; }
    .tab.active { color: #1f6feb; border-color: #1f6feb; }
    .login-body { padding: 24px 28px; }
    form { display: grid; gap: 12px; }
    input { padding: 12px; font-size: 16px; }
    button { padding: 12px; font-size: 16px; }
    .links { display:none; }
    .error { color: #b30000; }
  </style>
</head>
<body>
  <% if (site && site.admissionBg) { %>
    <img class="admission-bg" src="<%= site.admissionBg %>" alt="admission" />
  <% } %>
  <div class="login-card">
    <div class="login-header">
      <h1 class="login-title">次元学院 登录</h1>
      <div class="tabs">
        <div class="tab <%= role === 'parent' ? 'active' : '' %>" onclick="switchRole('parent')">家长登录</div>
        <div class="tab <%= role === 'admin' ? 'active' : '' %>" onclick="switchRole('admin')">管理员登录</div>
      </div>
    </div>
    <div class="login-body">
      <% if (error) { %><p class="error"><%= error %></p><% } %>
      <form id="form-parent" method="post" action="/login" style="display:<%= role==='parent'?'grid':'none' %>">
        <label>学号</label>
        <input name="studentId" required />
        <label>密码</label>
        <input type="password" name="password" required />
        <button type="submit">登录</button>
      </form>

      <form id="form-admin" method="post" action="/admin/login" style="display:<%= role==='admin'?'grid':'none' %>">
        <label>用户名</label>
        <input name="username" required />
        <label>密码</label>
        <input type="password" name="password" required />
        <button type="submit">登录</button>
      </form>

      <div class="links"></div>
    </div>
  </div>

  <script>
    function switchRole(role){
      const url = new URL(window.location.href);
      url.searchParams.set('role', role);
      window.location.href = url.toString();
    }
  </script>
</body>
</html>


