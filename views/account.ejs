<!DOCTYPE html>
<html lang="zh-CN">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>账户设置</title>
  <link rel="stylesheet" href="/styles.css" />
</head>
<body>
  <div class="container">
    <h1>账户设置</h1>
    <p>学号：<%= parent.studentId %></p>
    <div class="toolbar">
      <a href="/records"><button>返回档案</button></a>
      <form method="post" action="/logout" style="display:inline-block"><button type="submit">退出</button></form>
    </div>

    <% if (error) { %>
      <p style="color:#b91c1c"><%= error %></p>
    <% } %>
    <% if (success) { %>
      <p style="color:#15803d"><%= success %></p>
    <% } %>

    <div style="display:grid; gap:16px; max-width:520px">
      <form method="post" action="/account" style="display:grid; gap:10px">
        <input type="hidden" name="action" value="updateName" />
        <label>家长名称</label>
        <input name="name" value="<%= (typeof user.name !== 'undefined') ? user.name : '' %>" style="padding:12px; font-size:16px" />
        <button type="submit">保存名称</button>
      </form>

      <form method="post" action="/account" style="display:grid; gap:10px">
        <input type="hidden" name="action" value="updatePassword" />
        <label>原密码</label>
        <input name="currentPassword" type="password" value="<%= (typeof form && form.currentPassword) ? form.currentPassword : '' %>" style="padding:12px; font-size:16px" />
        <label>新密码</label>
        <input name="newPassword" type="password" value="<%= (typeof form && form.newPassword) ? form.newPassword : '' %>" style="padding:12px; font-size:16px" />
        <label>确认新密码</label>
        <input name="confirmPassword" type="password" value="" placeholder="请再次输入新密码" style="padding:12px; font-size:16px" />
        <button type="submit">保存密码</button>
      </form>
    </div>
  </div>
</body>
</html>


